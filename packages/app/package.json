{
  "name": "@gubble/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The instrument. GRID + mixer at M2: canvas render with a <pre> mirror as the copy source of truth, XY pad, rail, density/grain/phase live.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@gubble/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
