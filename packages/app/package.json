{
  "name": "@gubble/app",
  "version": "0.0.0",
  "private": true,
  "description": "The instrument. Vite, minimal chrome, GRID + FLOW. Arrives at M2 (spec §17) — this package is a placeholder so the workspace shape exists before the code does.",
  "scripts": {
    "build": "echo '@gubble/app arrives at M2 — see GUBBLE-SPEC.md §17' && exit 0"
  }
}
