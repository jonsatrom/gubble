{
  "name": "gubble",
  "private": true,
  "version": "0.0.0",
  "description": "A drawing app for digital detritus. See GUBBLE-SPEC.md.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "verify": "npm run build --workspace=@gubble/core && npm run test --workspace=@gubble/core && tsc -p packages/app/tsconfig.json --noEmit",
    "compile": "npm run compile --workspace=@gubble/cli --"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
