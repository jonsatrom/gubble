{
  "name": "@gubble/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The instrument: GRID + FLOW performances of one buffer, XY mixer, selection verbs + spawnable controllers, doc/kit/aesthetic URLs with fork lineage, FREEZE/print. See root README's ledger for what's alive vs absent.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@chenglou/pretext": "0.0.8",
    "@gubble/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
